services:
  - type: web_service
    name: nanny_guard
    env: docker
    branch: main
    dockerfilePath: ./Dockerfile
    startCommand: sh -lc 'php artisan migrate --force && php artisan serve --host 0.0.0.0 --port 8000'
    plan: free
